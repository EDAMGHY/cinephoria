import { View } from "react-native";
import { Text } from "~/components/ui";

const ProfileScreen = () => {
  return (
    <View className="p-10">
      <Text className="text-card-foreground">ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
