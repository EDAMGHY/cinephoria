import { View } from "react-native";
import { Text } from "~/components/ui";

const AgendaScreen = () => {
  return (
    <View className="p-10">
      <Text className="text-card-foreground">AgendaScreen</Text>
    </View>
  );
};

export default AgendaScreen;
